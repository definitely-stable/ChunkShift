# ChunkShift

ChunkShift v0.1 is a deterministic manifest/diff/verify SDK for .NET.

It is **not** a backup server, sync client, chunk store, or encryption layer.

## Features

- Deterministic chunking algorithms
- Manifest generation for chunk verification
- Diff capabilities between manifests
- Multiple profile support (FastCDC, FixedSize)

## License

MIT